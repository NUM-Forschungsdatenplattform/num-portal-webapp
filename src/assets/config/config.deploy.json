{
    "env": {
        "name": "#{env_name}"
    },
    "api": {
        "url": "#{api_url}"
    },
    "auth": {
        "baseUrl": "#{auth_baseUrl}",
        "realm": "#{auth_realm}",
        "clientId": "#{auth_clientId}"
    }
}