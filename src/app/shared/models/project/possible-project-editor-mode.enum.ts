import { ProjectMenuKeys } from 'src/app/modules/projects/components/projects-table/menu-items'

export enum PossibleProjectEditorMode {
  EDIT = ProjectMenuKeys.Edit,
  PREVIEW = ProjectMenuKeys.Preview,
  REVIEW = ProjectMenuKeys.Review,
}
